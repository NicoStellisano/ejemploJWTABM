import { DirectivaDirective } from './directiva.directive';

describe('ColorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaDirective();
    expect(directive).toBeTruthy();
  });
});
